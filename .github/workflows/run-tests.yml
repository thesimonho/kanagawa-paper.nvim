name: Run Tests
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  tests:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ssh-key: ${{ secrets.DEPLOY_KEY }}
      - name: Fetch PR merge commit (only on PR)
        if: github.event_name == 'pull_request'
        run: git fetch origin +refs/pull/${{ github.event.pull_request.number }}/merge
      - name: Check for test-relevant changes
        id: test-changes
        run: |
          echo "Checking for test-relevant changes..."
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "run-tests=true" >> $GITHUB_OUTPUT
          elif git diff --name-only FETCH_HEAD^1 FETCH_HEAD | grep -E '(^scripts/test\.sh$|^\.github/workflows/run-tests\.yml$|\.lua$)'; then
            echo "run-tests=true" >> $GITHUB_OUTPUT
          else
            echo "run-tests=false" >> $GITHUB_OUTPUT
          fi
      - uses: rhysd/action-setup-vim@v1
        if: steps.test-changes.outputs.run-tests == 'true'
        with:
          neovim: true
      - name: Set permissions
        if: steps.test-changes.outputs.run-tests == 'true'
        run: chmod +x scripts/test.sh
      - name: Run test script
        if: steps.test-changes.outputs.run-tests == 'true'
        run: |
          scripts/test.sh || { echo "❌ Test script failed!"; exit 1; }
      - name: Mark test as skipped
        if: steps.test-changes.outputs.run-tests == 'false'
        run: echo "✅ No test-relevant changes, skipping tests."
